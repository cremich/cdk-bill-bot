// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for YesterdayFunction
 */
export interface YesterdayFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/daily-spends/yesterday.
 */
export class YesterdayFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: YesterdayFunctionProps) {
    super(scope, id, {
      description: 'src/daily-spends/yesterday.lambda.ts',
      ...props,
      runtime: lambda.Runtime.NODEJS_14_X,
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/daily-spends/yesterday.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}